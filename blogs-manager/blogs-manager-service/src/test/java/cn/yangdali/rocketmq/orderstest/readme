订单发送实例---MessageQueueSelector实现顺序消费
   	顺序消费是指消息的消费顺序和生产顺序相同，在某些场景下，必须保证顺序消息。比如订单的生成、付款、发货。
	顺序消息又分为全局顺序消息和部分顺序消息，全局顺序消息指某一个topic下的所有消息都要保证顺序执行；
								       部分顺序消息只要保证某一组消息被顺序消费
	对于订单消息来说。只要保证同一个订单ID的生成、付款、发货消息按照顺序消费即可