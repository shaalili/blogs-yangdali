简单发送实例
	1.同步发送，存在多条消息时，只有当上一条消息返回发送结果之后，才会继续发送消息。适用于订单，重要通知等
	2.异步发送消息，存在多条消息时，当上一条消息发送过后，直接进行下一条消息的发送，无需等待上一条消息返回结果。但是不等待并不意味着无返回结果。
	在发送过后，仍会返回对应的成功或者是异常信息。可以利用回调函数来对相应的信息进行处理
	3.单向发送，发送后不获取发送结果，即可能信息存在丢失状况。适用于中等可靠性的情况，例如日志收集等